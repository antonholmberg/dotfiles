general:
  import:
    - ~/.config/alacritty/catppuccin/catppuccin-mocha.yml
  live_config_reload: true

terminal:
  shell:
    program: "/usr/bin/env bash"
    args:
      - "--login"

env:
  TERM: "xterm-256color"

font:
  size: 12.0
    
